# Documentation build dependencies — single source of truth is pyproject.toml [docs].
# Install with: pip install -e ".[docs]"
#
# After installing, build the docs with:
#   cd docs && make html        (Linux/macOS)
#   cd docs && _build.bat       (Windows)
-e ".[docs]"
